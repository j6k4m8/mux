{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the local Mux mailbox window; app commands remain explicitly allowlisted in Rust",
  "windows": ["main"],
  "permissions": ["core:default"]
}
